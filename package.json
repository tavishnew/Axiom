{
    "scripts": {
        "dev": "concurrently \"pnpm -F @workspace/frontend dev\" \"pnpm -F @workspace/backend dev\"",
        "build": "pnpm -r run build",
        "start": "pnpm -F @workspace/backend start",
        "typecheck": "pnpm -r run typecheck",
        "lint": "pnpm -r run lint"
    },
    "devDependencies": {
        "concurrently": "^10.0.4",
        "cross-env": "^7.0.3",
        "typescript": "~5.9.3"
    }
}